import { View } from 'react-native';

type NoteImagePickerProps = {
  imageIds: string[];
  onChange: (imageIds: string[]) => void;
};

export function NoteImagePicker(_props: NoteImagePickerProps) {
  return <View />;
}
