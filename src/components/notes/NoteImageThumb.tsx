import { View } from 'react-native';

type NoteImageThumbProps = {
  imageId: string;
  size?: number;
  onRemove?: () => void;
};

export function NoteImageThumb(_props: NoteImageThumbProps) {
  return <View />;
}
